# config file for Coates gcc builds
Testing:
	PBS=True
	queue=prism
	walltime=4:00:00
	nodes=4
	ncpus=8
	modules=openmpi-gcc

lammps:
	Build=True
	modules=openmpi-gcc
	build=openmpi

fvm:
	Build=1

	# build using MPI
	# default is False
	parallel=True

	# 'release' or 'debug'
	# release is built with optimization on
	# default is release
	version=release

	# compiler. 'intelc' or 'gcc'
	# default is gcc
	compiler=gcc
	modules=openmpi-gcc

MPM:
	Build=1
	modules=atlas openmpi-gcc
	configname=gnu-linux


mpi4py:
	modules=openmpi-gcc

netcdf:
	Build=1
	env[F90]=gfortran
	env[CC]=gcc

parmetis:
	modules=openmpi-gcc

MEMOSA:
	Build=1
	
ALL:
	modules=gcc/4.3.0
	before=module load python
#	env[HDF5_DIR]=/apps/rhel5/graphics/x86_64/gnu/gcc-4.4.0/hdf5-1.8.3

