# config file for Coates gcc builds
Testing:
	PBS=True
	queue=prism
	walltime=4:00:00
	nodes=4
	ncpus=8
	modules=mpich2-gcc

lammps:
	Build=True
	modules=mpich2-gcc
	build=steele

fvm:
	Build=1

	# build using MPI
	# default is False
	parallel=True

	# 'release' or 'debug'
	# release is built with optimization on
	# default is release
	version=release

	# compiler. 'intelc' or 'gcc'
	# default is gcc
	compiler=gcc
	modules=mpich2-gcc

MPM:
	Build=1
	modules=mpich2-gcc
	configname=gnu-linux


mpi4py:
	modules=mpich2-gcc

netcdf:
	env[F90]=gfortran
	env[CC]=gcc

parmetis:
	modules=mpich2-gcc
	
ALL:
	modules=gcc atlas hdf5-1.8.3/gcc-4.4.0
	env[HDF5_DIR]=/apps/rhel5/graphics/x86_64/gnu/gcc-4.4.0/hdf5-1.8.3


