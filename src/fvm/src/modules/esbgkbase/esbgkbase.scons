Import('env')

src = ['KineticModels.cpp']
deps=['fvmbase','importers','exporters', 'rlog']

#env.createATypedSharedLibrary('esbgkbase',src,['rlog','fvmbase'])

#deps += ['esbgkbase']

env.createATypedSwigModule('esbgk_atyped',sources=['esbgk.i'],deplibs=deps)

#env.createExe('testquadrature',src, deplibs=deps)

