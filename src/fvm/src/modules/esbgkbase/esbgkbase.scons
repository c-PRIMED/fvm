Import('env')

src = ['testquadrature.cpp',
       ]
deps=['fvmbase','importers','exporters', 'rlog']

env.createSharedLibrary('esbgkbase',src,['rlog','fvmbase'])

deps += ['esbgkbase']
env.createSwigModule('Quadrature',sources=['Quadrature.i'],deplibs=deps)
env.createSwigModule('KineticModel',sources=['KineticModel.i'],deplibs=deps)

env.createExe('testquadrature',src, deplibs=deps)

