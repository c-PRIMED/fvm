Import('env')

src = [
    'NcDataWriter.cpp'
    ]

env.createSharedLibrary('exporters',src,['rlog','fvmbase'])
env.createATypedSwigModule('exporters_atyped',sources=['FluentDataExporter.i'],
                           deplibs=['fvmbase','exporters','importers','rlog'])
